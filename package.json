{
  "scripts": {
    "dev": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "build": "tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify && cp -r public dist/ && cp -r assets dist/ && mkdir -p dist/mask && cp -r src/mask/* dist/mask/ && mkdir -p dist/animation && cp -r src/animation/* dist/animation/",
    "vercel-build": "next build",
    "start": "serve dist/"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "serve": "^14.2.1"
  }
}
